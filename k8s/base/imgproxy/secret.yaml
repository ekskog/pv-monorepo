apiVersion: v1
kind: Secret
metadata:
  name: imgproxy-secret
  namespace: pv
type: Opaque
stringData:
  # Same MinIO credentials used by pv-api — apply via kubectl, do NOT commit real values.
  # kubectl create secret generic imgproxy-secret \
  #   --from-literal=AWS_ACCESS_KEY_ID="<minio-access-key>" \
  #   --from-literal=AWS_SECRET_ACCESS_KEY="<minio-secret-key>" \
  #   --namespace=pv --dry-run=client -o yaml | kubectl apply -f -
  AWS_ACCESS_KEY_ID: "REDACTED"
  AWS_SECRET_ACCESS_KEY: "REDACTED"
